<%= form_with(model: eraser, local: true) do |form| %>
  <% if eraser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eraser.errors.count, "error") %> prohibited this eraser from being saved:</h2>

      <ul>
      <% eraser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :eraser_id %>
    <%= form.number_field :eraser_id, id: :eraser_eraser_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :eraser_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :eraser_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
