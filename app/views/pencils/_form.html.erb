<%= form_with(model: pencil, local: true) do |form| %>
  <% if pencil.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pencil.errors.count, "error") %> prohibited this pencil from being saved:</h2>

      <ul>
      <% pencil.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pencil_id %>
    <%= form.number_field :pencil_id, id: :pencil_pencil_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :pencil_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :pencil_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
